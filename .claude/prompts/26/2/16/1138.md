1. `ACCOUNT_TYPES` 리스트 추가 (계정과목 목록을 한 곳에서 관리)
2. `account_types` 테이블 신규 생성 (id, account_nm UNIQUE)
3. `financial_statements` 테이블에서 `account_nm VARCHAR(200)` → `account_type_id INTEGER REFERENCES account_types(id)` FK로 변경

이런 변화가 생겼습니다. 적용해주세요